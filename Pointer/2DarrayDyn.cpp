#include<stdio.h>
#include<stdlib.h>
int main(){
	int i,j,row;
	int (*a)[4]
	printf("enter row");
	scanf("%d",&row);
	a=(int (*)[4])calloc(row*4,sizeof(int));
	if(p==NULL){
		printf("Not Enough Space");
		exit(1);
	}
	//for loop assign the array a[i][j]
}
